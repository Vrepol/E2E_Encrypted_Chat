## To do list

增加一个config文件（加密的，密钥为程序中写死的常量），能够做到打开即用

密钥可以用房间号和自定义密码生成MD5码，作为房间内部的本地密钥，在第一次建立房间时（房主会发送一段加密（MD5加密后）的“hello”密文，放在服务器上保存，后续成员输入密码连接至房间后会自动发送一段本地生成的“hello”密文，服务器确保一致后将其加入房间，否则断开连接，客户端回到房间选择界面
（同时为了时效性，可以在md5生成时加上日期信息）

到底是实例化房间还是实例化对话

房间列表（人数）

房间内部（在线名单）

自定义服务器功能

自定义昵称和随机昵称

发送图片